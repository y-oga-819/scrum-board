# CLAUDE.md

このリポジトリで作業する AI エージェント（Claude Code）向けの作業指針。
プロジェクトの全体像は [`README.md`](./README.md)、設計の正は
[`docs/proposal.html`](./docs/proposal.html)、実装進捗は
[`docs/progress.md`](./docs/progress.md)、設計判断は
[`docs/decisions/`](./docs/decisions/)（D-NN）にある。

## よく使うコマンド

```bash
make dev         # フロント + バックエンドを同時起動（開発）
make test        # 全テスト（pytest + Vitest）
make lint        # 全 Lint（ruff / ESLint）
make typecheck   # 全型チェック（mypy / tsc）
make build       # 本番相当の SPA バンドルをビルド
```

## コミットの粒度

**1 PR = 1 コミット にしない。** 変更が数百行に及ぶ場合、1 つの巨大コミットに
まとめるのはレビューしづらく、履歴からも意図が読み取れない。
**「レビュアーが上から順に読める物語」になるようコミットを分ける。**

### 原則

- **1 コミット = 1 つの完結した論理的変更。** そのコミット単体で説明が付き、
  ビルド・テストが通る状態を保つ（`git revert` が意味を持つ単位）。
- **依存の順に積む。** 土台 → その上に乗るもの、の順に並べる。後ろのコミットが
  前のコミットに依存するのは良いが、逆流させない。
- **種類を混ぜない。** 「機能追加」と「無関係なリファクタ」「フォーマット修正」
  「依存更新」は別コミットにする。差分ノイズでレビューを埋めない。
- **コミットメッセージは Why を書く。** 何を変えたかは差分でわかる。なぜそう
  したかを 1 行目の後に残す。

### 分け方の目安（この順で積むと読みやすい）

1. **準備・下ごしらえ** — 既存コードの整理、移動、リネーム（挙動を変えない）
2. **土台・スキーマ・型・ポート定義** — 後続が依存するインターフェース
3. **中核ロジックの実装** — 本命の変更
4. **配線・エンドポイント・UI** — 中核を外から使えるようにする
5. **テスト** — 実装と同じコミットに含めてもよいが、大きい場合は分ける
6. **ドキュメント・設定・進捗更新**（README / docs/decisions / docs/progress.md）

> 迷ったら「このコミットだけを revert したら何が戻るか」を自問する。
> 答えが 1 文で言えないなら、粒度が粗すぎる。

### コミットメッセージの書式

既存の履歴に合わせ、Conventional Commits 風の日本語プレフィックスを使う。
PBI に対応する作業なら本文に ID（例: `B-07`）を含める。

```
feat: B-07 データアクセス基盤（Repository ポート／共通処理）
refactor: Cosmos クライアントをシングルトン化する器を用意
fix: MSAL protectedResourceMap を /api/* にする
docs: M1 達成。B-02〜B-06 を完了に更新
ci: deploy を paths で絞る
```

プレフィックスの目安: `feat` / `fix` / `refactor` / `docs` / `test` / `ci` /
`chore` / `style`。

### 実務上の進め方

- 大きな変更に着手する前に、**どういうコミット列にするかを先に見積もる**
  （上の「分け方の目安」に当てはめる）。
- まとめてコードを書いてしまった場合でも、`git add -p` で論理単位ごとに
  ステージして分割コミットする。1 発コミットで済ませない。
- 各コミットを積む前に、可能なら `make test` / `make typecheck` が通る状態を保つ。

## プルリクエスト

PR の説明は [`.github/PULL_REQUEST_TEMPLATE.md`](./.github/PULL_REQUEST_TEMPLATE.md)
に従う。差分でわかる「何を変えたか」ではなく、**なぜ・考え方がどう変わったか・
リリースで気をつけること**を書く。
