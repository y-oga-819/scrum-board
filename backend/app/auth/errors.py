"""認証まわりの例外。

トークン検証（V-1〜V-4）やヘッダの不備を 1 つの例外型で表す。API 層はこれを
捕まえて 401 に変換する（D-20: 401 は「未認証・トークン不正」）。理由文字列は
ログ／デバッグ用であり、原因（署名不正・aud 不一致・スコープ欠落など）を
そのまま利用者に返すと情報が漏れるため、応答本文には汎用メッセージを使う。
"""

from __future__ import annotations


class InvalidTokenError(Exception):
    """アクセストークンが提示されない、または検証に失敗した。"""
