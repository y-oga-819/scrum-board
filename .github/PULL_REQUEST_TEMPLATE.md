<!--
記入のコツ:
- コードを読めばわかる「何を変えたか」は書かない（差分を見れば済む）
- 「なぜ」「どう考え方が変わったか」「リリースで気をつけること」を残す
- 不要なセクションは削除して構わない
-->

## 概要 (What)

<!-- 1-3 行。このPRが何を達成するか。対応する PBI があれば ID を書く（例: B-07）。 -->

## 関連 Issue / PBI

<!-- 対応 issue があれば必ずリンクする（マージ時自動 close したい場合は `Closes #N`）。
     参照するだけ（close しない）なら `Refs #N`。
     複数 close する場合は `Closes #1, Closes #2` のように `Closes` を都度書く。
     PBI ベースの作業なら docs/progress.md の該当 ID（例: B-07）も併記する。
     対応 issue / PBI が無い PR の場合はこのセクションごと削除して構わない。 -->

Closes #

## 背景 (Why)

<!-- なぜこの変更が必要だったか。トリガーになった出来事 / 制約 / 課題。
     関連する設計判断があれば docs/decisions/D-NN をリンク（例: D-21）。
     設計の正は docs/proposal.html にある。 -->

## Before → After (概念・フロー・メンタルモデル)

<!-- 実装の差分ではなく、考え方やフローがどう変わったかを書く。
     例: 「ユーザーは X を意識する必要があったが、今は不要」「データの所有者が A から B に移った」など。
     必要なら Mermaid で図示。 -->

**Before:**

**After:**

## 追加したテスト (How verified)

<!-- 何をどう保証したか。ユニット / 統合 / E2E / 手動確認 など（`make test` / `make typecheck`）。
     カバーできていない領域があれば正直に書く。 -->

- [ ]

## リリース前チェックリスト (When / Before merge)

<!-- マージ・デプロイ前に必要な作業。該当しないものは削除。 -->

- [ ] データストア（Cosmos）の初期化 / 移行の適用順を確認した（docs/decisions/D-21）
- [ ] 環境変数 / シークレット（Entra ID などの認証情報を含む）を追加・更新した
- [ ] ドキュメント (README / docs/decisions / docs/progress.md) を更新した
- [ ] 既存ユーザー・既存データへの影響（破壊的変更）を確認した

## このPRの範囲外 (Out of scope)

<!-- 意図的にやらなかったこと / 別 issue に切り出したこと。
     レビュアーの「これも直さないの？」を先回りで潰す。 -->
