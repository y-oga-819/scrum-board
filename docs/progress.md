# スクラムバックログ管理アプリ — 開発進捗管理

> このアプリ自身でPBI管理ができるようになる（マイルストーン **M3** 到達）までの間、
> AI開発の進捗をこのファイルで管理する。設計の正はあくまで
> [`docs/proposal.html`](./proposal.html)（設計提案書）にあり、本ファイルはその
> 実装進捗のトラッカーである。

- **設計原則**（迷ったらここに戻る／提案書 01章）
  - **P-1** ルールで縛らず、事実を見せる
  - **P-2** 管理コストを下げる
  - **P-3** 後から復元できない情報だけを先に持つ
- **完了条件の考え方**（提案書 12章）: 「機能が動くか」ではなく
  **設計判断が実装に落ちたか**を確認できる形で書く。

---

## このファイルの使い方

- 各PBIは `未着手` → `進行中` → `完了` の3状態で管理する（アプリの `todo/doing/done` に対応）。
- 進捗の実体は各PBIの**完了条件チェックリスト**にある。チェックが全部埋まって初めて `完了`。
- `B-01`〜`B-24` は提案書由来のPBI。`B-00` と `B-25`以降は
  **設計レビューで抽出した着手前の欠落**を埋めるための追加PBI（末尾に判断根拠を記載）。
- 着手順を縛るのは**依存関係だけ**（提案書 12章）。`依存` 列を見て着手可能なものから進める。

### ステータス凡例

| 記号 | 状態 | 意味 |
|:---:|:---|:---|
| ⬜ | 未着手 | 未着手。依存が解けていれば着手可能 |
| 🟨 | 進行中 | 着手済み。完了条件の一部が未達 |
| ✅ | 完了 | 完了条件をすべて満たした |
| 🔒 | ブロック | 依存または未決事項で着手できない |

---

## サマリ

| マイルストーン | 到達点 | PBI | 進捗 |
|:---:|:---|:---|:---:|
| **M0** | 開発の土台（テスト/CI/規約） | B-00, B-25, B-29 | 0 / 3 |
| **M1** | サインインして自分のデータが1件表示できる（歩ける骨格） | B-01〜B-07, B-26 | 0 / 8 |
| **M2** | プロダクトバックログが運用できる | B-08〜B-14, B-27 | 0 / 8 |
| **M3** | スプリントが1周回る（★ここからドッグフーディング） | B-15〜B-19, B-28 | 0 / 6 |
| **M4** | デイリースクラムがこの画面だけで完結する | B-20〜B-22 | 0 / 3 |
| **M5** | 実運用に耐える | B-23〜B-24 | 0 / 2 |
| | | **合計** | **0 / 30** |

> ★ **M3を終えた時点で、このバックログ自体をこのアプリで管理できる状態になる。**
> そこから先はドッグフーディングで進める。

---

## 着手前に必ず方針を決める（Open Questions）

以下は新規PBIに落としてあるが、着手判断に直結するため一覧でも掲げる。

| # | 論点 | 対応PBI | いつまでに |
|:---:|:---|:---:|:---|
| ~~Q-A~~ | ~~テスト戦略~~ → **決定済み** [`D-19`](./decisions/D-19-test-strategy.md) | B-00 | ✅ 2026-07-25 |
| Q-B | `productId` の発生源とデータseeding | B-26 | M1中 |
| Q-C | 認可ブートストラップ（最初のメンバー登録経路） | B-27 | B-14着手前 |
| ~~Q-D~~ | ~~API共通規約~~ → **決定済み** [`D-20`](./decisions/D-20-api-conventions.md) | B-25 | ✅ 2026-07-25 |
| Q-E | Cosmos の `ORDER BY` が辞書順と一致するか（提案書 Q-1） | B-09 | B-09の最初の作業 |
| Q-F | 楽観排他 412 発生後のUX（再取得マージ／再操作促し） | B-28 | B-17着手前 |

### 決定の記録

方針が固まったものは `docs/decisions/` に残す。提案書10章の **D-シリーズを継続**しており、
番号空間は1つ（提案書が「仕様を変更したくなったら、まずこの表を引く」と定めているため）。

| # | 決定 | 対応PBI |
|:---:|:---|:---:|
| [D-19](./decisions/D-19-test-strategy.md) | テスト戦略とCI基盤（4層構成・エミュレータ・カバレッジ方針） | B-00 |
| [D-20](./decisions/D-20-api-conventions.md) | API共通規約（読み書き非対称・RFC 9457・`If-Match`必須・型生成） | B-25 |

---

## M0 — 開発の土台

### ⬜ B-00 テスト戦略とCI基盤の確立　`依存: B-03`
> レビュー由来。提案書は「順序の破損は発見が遅れる」「壊れたことに気づきにくい」と
> 繰り返す。退行検知の仕組みがなければ、以降のPBIの完了条件は担保できない。
>
> **方針決定済み** → [`docs/decisions/D-19-test-strategy.md`](./decisions/D-19-test-strategy.md)
> pytest / Vitest / Playwright。Cosmos契約テストはエミュレータ、
> **照合順序の検証だけは実サービス**（B-09）。カバレッジは数値目標を置かない。

- [ ] pytest（層1・2）と Vitest（フロント単体）が導入され、1コマンドで走る
- [ ] **層1・2が Cosmos なしで走る**（Repositoryフェイクを使用 → B-07 と対で実施）
- [ ] 不変条件 I-1〜I-7 の**テーブル駆動**テスト雛形がある
- [ ] V-1〜V-4 をテスト用鍵ペア＋JWKSスタブで検証している（実テナントに接続しない）
- [ ] Cosmos契約テスト（412／バッチ原子性／RU）がエミュレータ上で走る
- [ ] エミュレータの起動待ちが**固定sleepではなくreadyポーリング**である
- [ ] Playwright で主要フロー5本が通る（サインイン／プランニング／ボード／終了処理／未割当）
- [ ] GitHub Actions で毎PR実行され、**失敗でマージがブロックされる**
- [ ] 必須4領域（I-1〜I-7／V-1〜V-4／rank／認可）の合意が文書化されている

### ⬜ B-25 API共通規約の策定　`依存: B-06`
> レビュー由来。最初のCRUD（B-08）が後続の暗黙の規約になる。エラー応答・信頼境界・
> 型共有を先に決めないと、PBIごとに形式がばらつく。
>
> **方針決定済み** → [`docs/decisions/D-20-api-conventions.md`](./decisions/D-20-api-conventions.md)
> 読み=画面単位 / 書き=リソース単位の非対称API。RFC 9457。`If-Match` 必須。

- [ ] RFC 9457（`application/problem+json`）が共通のエラー応答として動く
- [ ] `violations` に**不変条件ID（I-4 等）が機械可読で載る**
- [ ] ステータスコードの割当（401 / 403 / 404 / 409 / **412** / **428** / 422）が実装されている
- [ ] `PATCH`/`DELETE` で **`If-Match` 欠落を428で弾く**（無条件更新の経路が存在しない）
- [ ] 単一ドキュメント応答が `ETag` を返し、集約GETは各要素に `_etag` を含む
- [ ] **サーバーを信頼境界とする**と明記（フロントのバリデーションはUX補助、正はAPI）
- [ ] OpenAPI から TS型を生成し、**CIで生成物の差分を検出**する
- [ ] クエリ規約（論理削除は常時除外・ページングなし・`ORDER BY rank, id` はサーバー保証）

### ⬜ B-29 リポジトリ規約の整備　`依存: —`
> レビュー由来。M3のドッグフーディング前に最低限が要る。

- [ ] LICENSE を配置した
- [ ] README に開発セットアップ手順を書いた
- [ ] コミットメッセージ規約・ブランチ戦略を決めた
- [ ] CONTRIBUTING（または開発ガイド）を用意した

---

## M1 — 歩ける骨格

### ⬜ B-01 Entra IDにアプリを登録する　`依存: —`
- [ ] 提案書 08章のチェックリスト全項目が完了
- [ ] プラットフォーム = **シングルページアプリケーション**（`Web`にしない → `AADSTS9002326`回避）
- [ ] スコープ `api://<clientId>/access_as_user` を公開
- [ ] マニフェスト `requestedAccessTokenVersion: 2`
- [ ] リダイレクトURI 3種を登録（本番SPA / `localhost:4200` / Easy Auth保険）
- [ ] テナントID・クライアントIDを控えた
- [ ] **手順が再実行可能な形（az CLI中心）で残っている**

### ⬜ B-02 Azureリソースを用意する　`依存: —`
- [ ] App Service F1 を作成
- [ ] Cosmos DB **無料レベル**を明示的に選択して作成
- [ ] **予算アラートを設定した**（Azureは自動の支出上限がない）
- [ ] 手順が再実行可能な形で残っている

### ⬜ B-03 プロジェクトの骨格を作る　`依存: —`
- [ ] Angular のビルド成果物を FastAPI が配信する
- [ ] フロントとAPIが同一 App Service に同居する構成
- [ ] ローカルで1コマンド起動できる

### ⬜ B-04 デプロイパイプラインを通す　`依存: B-02, B-03`
- [ ] main への push で App Service に自動デプロイされる
- [ ] 公開URLで表示できる

### ⬜ B-05 フロントエンドのサインイン　`依存: B-01`
- [ ] MSAL でサインインできる
- [ ] 未認証ユーザーはガードで弾かれる
- [ ] ローカルでも本番と同じ経路で動く（Easy Auth に依存しない）

### ⬜ B-06 APIのトークン検証　`依存: B-01`
- [ ] V-1 署名検証（JWKSの公開鍵でRS256・鍵はキャッシュ）
- [ ] V-2 `aud` がクライアントIDと一致
- [ ] V-3 `iss` が `https://login.microsoftonline.com/<tenantId>/v2.0`
- [ ] V-4 `scp` に `access_as_user` が含まれる
- [ ] 改ざんトークンで 401 を返す

### ⬜ B-07 データアクセス基盤　`依存: B-02`
- [ ] 単一コンテナを作成（PK `/productId`）
- [ ] インデックス除外パス（`description` `memo` `minutes`）を設定
- [ ] 共通フィールド付与（id=ULID / type / createdAt 等）が共通処理として動く
- [ ] 論理削除（`isDeleted`）と、**全クエリでの `NOT isDeleted` 除外**を共通化した
- [ ] `_etag` による楽観排他（`If-Match` / 412再取得）が共通処理として動く
- [ ] **Repositoryがポートとして抽象化され、テスト用フェイクに差し替えられる**（D-19）

### ⬜ B-26 product初期化とデータseeding　`依存: B-07`
> レビュー由来。パーティションキー `/productId` が全設計の土台なのに、product
> ドキュメントの発生源が提案書にない。M1の「自分のデータが1件表示」に到達する前提。

- [ ] product ドキュメントの生成経路を決定（初回サインイン時に自動生成 or 手動投入）
- [ ] **seeding がスクリプト化され冪等である**（E2Eがテストごとに初期状態を作り直すため — D-19）
- [ ] `productId` の払い出しルールを文書化した

---

## M2 — プロダクトバックログが運用できる

### ⬜ B-08 PBIのCRUD API　`依存: B-06, B-07`
- [ ] 作成・取得・更新・論理削除ができる
- [ ] 不正な状態遷移（new→ready→inProgress→done 以外）が弾かれる
- [ ] **`PATCH`/`DELETE` は `If-Match` 必須**（欠落は428・不一致は412 — D-20）

### ⬜ B-09 並び替え（rank）　`依存: B-07`
> **最初の作業（Q-E）**: 実データを10件ほど投入し `ORDER BY` の結果が辞書順と一致するか確認する。
> 通らなければ**浮動小数＋定期リバランスに切り替える**（この分岐はB-09の工数を膨らませ得ると認識しておく）。
>
> ⚠️ **この検証は実サービスで行う。エミュレータでの確認は不可**（D-19）。
> エミュレータと実サービスで照合順序が異なった場合、「テストが通っているのに本番で
> 並びが静かに壊れる」という最悪の形になる。一度きりの独立ゲートとして扱い、CIには載せない。

- [ ] **実サービス**で `ORDER BY` が辞書順と一致することを確認済み（不一致なら方式切替を記録）
- [ ] 文字列ランク（fractional indexing・ライブラリ利用・Base36）を採用
- [ ] 生成はサーバー側（**専用エンドポイントに前後の要素IDを渡す** — D-20）
- [ ] 1件を移動したとき更新ドキュメントが**1件だけ**である
- [ ] `ORDER BY rank, id`（ULIDのidをタイブレーカー）で全端末の並びが一致する

### ⬜ B-10 プロダクトバックログ画面　`依存: B-05, B-08, B-09`
- [ ] PBIが優先順位順に並ぶ
- [ ] ドラッグで並び替えできる
- [ ] ステータスを変更できる
- [ ] **`GET /backlog`（画面単位）で1往復。N+1にしない**（D-20）

### ⬜ B-11 PBI詳細　`依存: B-10`
- [ ] 概要を編集できる
- [ ] 完了条件のチェックリストを編集できる
- [ ] `estimate` を編集できる（**任意入力・未設定でも警告を出さない** — D-06）

### ⬜ B-12 PBIの分割　`依存: B-10`
- [ ] 分割で生成したPBIが `parentPbiId` で元を参照する
- [ ] 一覧から分割元を辿れる

### ⬜ B-13 タスクのCRUD　`依存: B-07, B-08`
- [ ] I-1〜I-5 のバリデーションが**単一の関数に集約**されている
- [ ] I-4: 種別判定は `pbiId` の有無ではなく **`taskType`** で行う
- [ ] `taskType='team'`（親PBIなし）でも作成できる

### ⬜ B-14 メンバー管理と認可　`依存: B-06, B-07`
- [ ] プロダクトのメンバーでないユーザーは 403
- [ ] ユーザー識別子に `oid` を使っている（メールアドレスをキーにしない）

### ⬜ B-27 認可ブートストラップ（最初のメンバー登録）　`依存: B-26, B-14`
> レビュー由来。B-14は「メンバーでなければ403」。最初のメンバーが誰も居ない状態を
> どう抜けるか（鶏卵問題）を決めないと全員403で詰む。B-14と対で解く。

- [ ] 最初のユーザーを member（admin）として登録する経路を決定
- [ ] Entra側の「割り当て」とアプリ内 member の役割分担を文書化した
- [ ] メンバー0人の状態から正常にサインイン→利用開始できることを確認

---

## M3 — スプリントが1周回る（★ここからドッグフーディング）

### ⬜ B-15 スプリントのCRUD　`依存: B-07`
- [ ] 期間とゴールを設定できる
- [ ] `planned` / `active` / `closed` が遷移する

### ⬜ B-16 プランニングモード（右ペイン）　`依存: B-10, B-13, B-15`
- [ ] PBIのチェックで配下の未完了タスクに `sprintId=S` が付く
- [ ] **タスク0件のPBIをチェックすると「タスク分解」タスクが1件生成される**（D-15）
- [ ] 外すと**未完了タスクのみ** `sprintId=null` に戻る（完了タスクは動かさない — I-5）
- [ ] 上記の規則が**専用エンドポイントとしてサーバー側に閉じている**（D-20）

### ⬜ B-17 スプリント画面のボード　`依存: B-13, B-15`
- [ ] todo / doing / done をドラッグで移動できる
- [ ] ブロック中フラグを立てられる
- [ ] **2人が同時に触っても片方の更新が消えない**（楽観排他）
- [ ] **`GET /board`（画面単位）で1往復**。各要素に `_etag` が含まれる（D-20）

### ⬜ B-18 進捗表示　`依存: B-17`
- [ ] 提案書 05章の**2本バー**（計画タスク / チームタスク）が表示される
- [ ] マーカーが**営業日**で計算されている（暦日にしない）
- [ ] **警告色を使っていない**（色は種別の区別のみ — P-1 / D-13）
- [ ] （要確認）祝日・タイムゾーンの扱いを決めた
- [ ] **「今日」が注入可能で、日付を固定してテストできる**（D-19。B-19の終了処理も同様）

### ⬜ B-19 スプリント終了処理　`依存: B-15, B-17`
- [ ] 持ち越し対象の一覧をプレビューしてから確定できる（`close/preview` と `close` を分ける — D-20）
- [ ] **完了タスクは移動しない**（未完了のみ次スプリントへ）
- [ ] 強制も警告もせず事実だけ見せる（P-1）

### ⬜ B-28 楽観排他（412）のUX方針と実装　`依存: B-17`
> レビュー由来。B-17は412の**検出**を保証するが、**発生後どうするか**が未定義。
> 朝会の同時操作が前提なので体験を左右する。

- [ ] 412発生時の振る舞いを決定（自動再取得マージ / ユーザーに再操作を促す 等）
- [ ] 決めた振る舞いがボード操作で一貫して適用される
- [ ] 更新が黙って消える経路が存在しないことを確認

---

## M4 — デイリースクラムがこの画面だけで完結する

### ⬜ B-20 デイリーパネル　`依存: B-17`
- [ ] アジェンダと議事録を編集できる
- [ ] **1日1ドキュメント**で保存される（肥大化と同時編集競合を避ける）

### ⬜ B-21 NextActionの表示　`依存: B-20`
- [ ] 前スプリントのアクションが今スプリントのパネルに出る
- [ ] 完了にできる

### ⬜ B-22 未割当のチームタスク　`依存: B-10, B-16`
- [ ] スプリントから外したチームタスクがバックログ画面に現れる
- [ ] **どこにも表示されない状態が存在しない**（唯一許容できない失敗 — D-16）

---

## M5 — 実運用に耐える

### ⬜ B-23 ベロシティ表示　`依存: B-19`
- [ ] `completedSprintId` ごとの `estimate` 合計が出る
- [ ] 未設定の `estimate` は除外される
- [ ] **3スプリントの移動平均**で表示される（Q-3）

### ⬜ B-24 本番運用の確認　`依存: B-04, 全機能`
- [ ] F1のスリープからの復帰時間を実測した
- [ ] RU消費を実測し、無料枠に収まることを確認した
- [ ] 予算アラートが発火することを確認した

---

## 付録 — レビューで抽出した追加PBIの根拠

本ファイルの `B-00 / B-25 / B-26 / B-27 / B-28 / B-29` は提案書には無く、
「開発着手にあたって足りない部分」の設計レビューから起こした。対応関係は以下。

| 追加PBI | 元の懸念（重要度） | 要旨 |
|:---:|:---|:---|
| B-00 | 🔴 Blocker | テスト戦略が空白。不変条件・認証・rankの退行を検知する仕組みが無い |
| B-26 | 🔴 Blocker | `productId` の発生源とseedingが未定義。全設計の土台なのにPBIに無い |
| B-27 | 🔴 Blocker | 認可のブートストラップ（最初のメンバー）が未定義。全員403で詰む鶏卵問題 |
| B-25 | 🟠 設計の穴 | API共通規約（エラー応答・信頼境界・型共有）が未定。最初のCRUDが暗黙規約化する |
| B-28 | 🟠 設計の穴 | 楽観排他412の**発生後UX**が未定義。検出はするが振る舞いが無い |
| B-29 | 🟡 品質/運用 | LICENSE/README/コミット規約等が未整備。ドッグフーディング前に必要 |

既存PBIへ**注記のみ**で反映した懸念（新規PBIにはしていない）:

- **rankのフォールバック分岐**（🟠）→ B-09 の冒頭注記（Q-E）
- **論理削除の全クエリ除外**（🟡）→ B-07 の完了条件に共通化を追加
- **日付・タイムゾーン・祝日**（🟡）→ B-18 の完了条件に確認項目を追加

---

_最終更新: 2026-07-24_
